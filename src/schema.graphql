# import * from './generated/prisma.graphql'

type Mutation {
    createEvent(title: String!, description: String!, price: Int, start: String!, end: String!, image: String): Event!
}

type Query {
    events: [Event]!
}